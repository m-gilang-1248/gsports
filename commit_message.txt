feat: Initialize Firebase for Android

- Successfully logged in to Firebase and set the active project to 'gsports-32f7f'.
- Initialized Firestore rules in `firebase.json`.
- Generated `lib/firebase_options.dart` and registered the Android app in the Firebase project using `flutterfire configure`.
- Updated `lib/main.dart` to uncomment Firebase initialization with `DefaultFirebaseOptions.currentPlatform` and `dotenv.load()`.
- Ran `dart fix`, `analyze_files`, and `dart format` to clean up the code.
- Updated `IMPLEMENTATION.md` to reflect the completed Firebase setup for Android.